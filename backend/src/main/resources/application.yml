spring:
  lifecycle:
    timeout-per-shutdown-phase: 10s
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ema
    username: user
    password: password

server:
  port: 8082
  shutdown: graceful
