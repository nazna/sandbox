spring:
  lifecycle:
    timeout-per-shutdown-phase: 10s
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ema?useSSL=false
    username: root
    password: password

server:
  port: 8082
  shutdown: graceful
